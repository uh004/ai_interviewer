# 🤖 AI Interviewer
이력서 기반 **LLM + LangGraph** 대화형 AI 면접 시뮬레이터

## 📘 Project Overview

**AI Interviewer**는 이력서(PDF/DOCX)를 입력하면  
AI가 실제 면접관처럼 질문을 생성하고, 지원자의 답변을 **구체성·일관성·적합성·논리성** 기준으로 평가한 뒤  
**종합 피드백 보고서까지 자동 생성하는 End-to-End 면접 시스템**입니다.

- ✅ 이력서 텍스트 추출(PDF/DOCX)
- ✅ 이력서 요약 + 핵심 키워드 추출
- ✅ 경험/동기/논리 기반 질문 전략 생성
- ✅ 답변 평가(0/1 이진) + 누적 평균 점수 계산
- ✅ 기준 충족 시 다음 분야 전환, 미달 시 심화 질문 생성
- ✅ 면접 종료 시 최종 리포트 자동 생성 (LLM 총평 포함)
- ✅ Gradio 채팅 UI 제공

## ✨ Key Features

### 1) Resume Preprocess
- PDF/DOCX에서 텍스트 추출
- LLM 기반 요약(summary) 및 키워드(keywords) 생성
- 3명의 면접관(A/B/C) 관점 질문 전략 생성 후 **실제 질문 문자열만 사용**

### 2) Interactive Interview (Chat)
- Gradio 챗봇 UI로 질문/답변 진행
- 답변 입력 → LangGraph가 한 턴 실행 → 다음 질문 또는 종료 리포트 반환

### 3) Answer Evaluation (0/1 Scoring)
답변을 아래 4개 기준으로 독립적으로 0/1 채점하고, 누적 평균으로 분야별 성과를 계산합니다.

| Criteria | Description |
|---|---|
| 구체성 | 수치/사실/역할/방법/성과가 명확한가 |
| 일관성 | 주장–근거–사례 흐름이 자연스러운가 |
| 적합성 | 질문 의도/직무/경험과 직접 연결되는가 |
| 논리성 | 원인 → 행동 → 결과 인과 구조가 명확한가 |

### 4) Final Feedback Report
- 경험/동기/논리 각 분야별 종합 점수 및 평균(0~1)
- 강점/개선필요 항목 표시
- LLM 기반 최종 총평(3문장 이내) 생성

## 🛠 Tech Stack

| Category | Technology |
|---|---|
| Language | Python 3 |
| LLM | OpenAI Chat Models (gpt-4.1-mini) |
| Orchestration | LangChain, LangGraph |
| UI | Gradio |
| Resume Parsing | PyMuPDF, python-docx |
| Env | python-dotenv |

## 📂 Folder Structure

```txt
AI_INTERVIEWER/
├── main.py
├── README.md
├── requirements.txt
└── src/
    ├── nodes/
    │   ├── preprocess.py      # 이력서 분석 + 질문전략 생성 + 첫 질문 세팅
    │   ├── evaluate.py        # 답변 평가 (구체성/일관성/적합성/논리성)
    │   ├── gen_question.py    # 심화 질문 생성
    │   ├── router.py          # 다음 단계 결정 (심화/전환/요약)
    │   ├── summarize.py       # 최종 리포트 생성
    │   └── graph.py           # LangGraph 구성
    ├── ui/
    │   └── gradio_app.py      # Gradio UI
    └── utils/
        ├── io.py              # PDF/DOCX 텍스트 추출
        ├── llm.py             # .env 로드 + ChatOpenAI 생성
        ├── state.py           # State(TypedDict) + Pydantic 스키마 정의
        └── state_ops.py       # State 보조 로직


```
## 🚀 How to Run (실행 방법)

### 🚀 1. 저장소 클론
```
!git clone https://github.com/uh004/ai_interviewer.git
%cd ai_interviewer
```

### 🔑 2. 환경 변수(.env) 설정
```
%%writefile .env
OPENAI_API_KEY=apikey입력
```

※ 위 명령 실행 후 .env 파일이 생성되면 직접 API KEY를 입력하세요.
- openai_api ---> https://openai.com/ko-KR/index/openai-api/


### 📦 3. Python 패키지 설치
```
!pip install -r requirements.txt
```

### ▶️ 4. 실행
```
!python main.py
```

### 📌 참고사항
```
이 실행 가이드는 **Google Colab 환경에서 실행하는 것을 기준**으로 작성되었습니다.
```
